{set:unix|{unix}} 
{if:{argslen}|=|0|then:{code:.t {tagname} <...ffmpeg_code>}|else:{sh:load {av}

ffmpeg -loglevel quiet -hide_banner -i $FILE_1 {args} ./output/$(basename $FILE_1) 
} 
SSP552FFmpeg done.
-# Took {math:fix(({unix}-{get:unix})/1000,3)} seconds.}
